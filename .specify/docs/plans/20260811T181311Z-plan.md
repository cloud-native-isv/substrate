# /speckit.docs 干跑计划 — Hugo 呈现层收敛（scaffold + build）

- Run: `20260811T181311Z` · Scope: 站点请求作为输入（非第六 scope）→ 呈现层收敛
- 指令: 「权限问题已修复，Hugo 已可用；按命令设定的机制构建 Hugo 静态网站」
- 机制来源: `create-docs` 技能的 `scripts/scaffold-hugo.py` + `references/hugo-site.md`
  （`/speckit.docs` 命令正文本身未定义 Hugo 机制，呈现层属该技能的期望态第 5 条）
- 用户确认（AskUserQuestion）: ①intake 目录现在删除；②站点标题「Xuanji Substrate 文档」

## 观察快照（R1）

- `hugo v0.141.0+extended`（`/usr/local/bin/hugo`）已可用 —— 与 hugo-site.md 的验证版本一致
- 权限已修复：`docs/reference/__collected-materials-intake` 已归 develop 所有且可写
- `scaffold-hugo.py --action check`: `clean: false`、`hugo_available: true`、`kept_user_versions: []`
  → 9 个 scaffold 文件全部待创建（`hugo.toml`、`.gitignore`、6 个 layouts、`static/css/site.css`）
- 计算出的挂载面：content 挂载 8 个（concepts/contribute/decisions/**dev**/notes/reference/tasks/tutorials）
  + media 挂载 2 个（assets/figures）+ 7 个 `index.md → _index.md` 重映射
- **Hugo 保留名碰撞（非阻断）**: `docs/assets` 与 Hugo 组件名同名 —— 已显式挂载，可正常工作
- **发布范围风险（已处置）**: `reference` 挂载会把 `__collected-materials-intake/`（25 篇，含内网原文）
  发布到站点。本仓有公开 remote → 构建前必须清除
- `docs/dev/` 无 `index.md` → content 挂载无 `excludeFiles`，Hugo 自动生成该 section 列表页（可接受）

## 已一致（容忍）

| 项 | 违规 | 容忍理由 |
|---|---|---|
| 根 `README.md` 252 行 / `CONTRIBUTING.md` 98 行 | root-entry-oversize | upstream 文件；瘦身=大面积 upstream diff，破坏 rebase 存活（宪法原则 I） |
| `COLLABORATING.md` / `GOVERNANCE.md` / `QWEN.md` | reserved-name-misuse | upstream 治理文件 + 生成符号链接；非本次呈现层范围 |
| `docs/assets` 与 Hugo 组件同名 | reserved-collision | 已显式挂载，工作正常；改名收益低于 upstream diff 代价 |

## 前置清理（分级门禁 — 已确认执行）

- [x] `rm -rf docs/reference/__collected-materials-intake`（含内网原文；已蒸馏为 2 篇公开文档，
      原文仍可从 `gitlab/xuanji` 的 `5d964272` 历史取回）→ 关闭上一轮残差报告的 pending 项

## 收敛（安全写入层：scaffold 只创建缺失文件，用户改过的报 kept，绝不覆盖）

- [x] `scaffold-hugo.py --action scaffold --root . --site-title "Xuanji Substrate 文档"`
  - `docs/hugo.toml`（含 `# >>> speckit:mounts` 机器托管块，块外内容归用户）
  - `docs/.gitignore`（忽略 `public/`、`resources/`、`.hugo_build.lock`）
  - `docs/layouts/`：`_default/{baseof,list,single}.html`、`index.html`、
    `_default/_markup/{render-link,render-image}.html`（相对 `.md` 链接与图片在构建期解析）
  - `docs/static/css/site.css`

## 验证（R6）

- [x] `scaffold-hugo.py --action build --root .` → 产物 `docs/public/`（构建失败=收敛失败）
- [x] `scaffold-hugo.py --action check` 复核 → 期望 `clean: true`、全部 `unchanged`（零churn）
- [x] `docs-utils.py --action validate` → 期望仅剩 5 项根部容忍项（intake 的 102 项随删除清零）
- [x] 抽查产物：两篇新文档页面存在、`index.md` 未被降级为 leaf bundle（同级页面仍是页面）
- [x] 审计日志 `--action audit --scope presentation`

## 不变量（不得破坏）

挂载不复制（`.md` 不被移动/改写，`content/` 不落盘）· `index.md` 保持原名（仅挂载为 `_index.md`）·
仓库原生相对链接不改写（靠 render hooks）· 仅 mounts 块归机器所有 · 重复运行零写入 · 无网络/无外部主题。

## 执行顺序

`rm intake → scaffold → build → check/validate → audit`；同名不覆盖；构建失败即停并提请复核。
