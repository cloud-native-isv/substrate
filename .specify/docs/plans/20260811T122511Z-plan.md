# /speckit.docs 干跑计划 — 扇出 intake（collected-materials 拆解归位）

- Run: `20260811T122511Z` · Scope: 单目标扇出（`docs/reference/collected-materials/`）
- 指令: 「按当前项目文档结构重新拆解和挪动；保留相关内容，不相关的直接清理」
- 用户确认（AskUserQuestion）: ①内网 18 篇 → 蒸馏 + 全部移出；②移出物 → 仓内 gitignored 目录；③蒸馏产出 2 篇正式文档

## 观察快照（R1）

- 25 个 tracked 文件：4 篇 `01-internal-project-progress/`（全文，含员工姓名/未公开性能数据/客户名）、14 篇 `02-internal-platform-practice/`（15 行摘要桩）、6 篇 `03-public-competitor-landscape/`（GitHub README 照录）+ 目录 `README.md`
- **保密风险**：本仓有公开 remote（`origin`/`github` → github.com）。内网原文照录入库 = 公开内网内容。当前 `5d964272`（add collected-materials）仅在 `gitlab/xuanji`；`github/xuanji` 落后一提交 → 尚未泄漏
- **权限约束**：整个 `collected-materials/` 树为 root(uid0) 所有。develop 用户可整体 `mv`（父目录 `docs/reference/` 可写），但**不能 `rm`/编辑树内文件**。sudo 不可用
- validate: 树内 101 broken-link + 1 reserved-name-misplaced（`collected-materials/README.md`）；树外 5 项（COLLABORATING/GOVERNANCE/QWEN/CONTRIBUTING/README）为既有 upstream 容忍项，不在本次范围
- 归属素材已代码核实：`cmd/e2bgw/internal/server/server.go`（E2B REST→ateapi 映射真实端点）

## 已一致（容忍）

| 项 | 违规 | 容忍理由 |
|---|---|---|
| 树外 5 项（README/CONTRIBUTING 超屏、COLLABORATING/GOVERNANCE/QWEN 保留名） | oversize / reserved-name | upstream / 生成物；历史计划已登记容忍，非本次单目标范围 |

## 蒸馏产出（写作 — 安全写入层，来源仅公开 URL + 本仓代码）

- [x] `docs/reference/e2b-api-surface.md` — E2B SDK/REST 端点清单 × `cmd/e2bgw` 覆盖矩阵（已实现/占位/未接）。来源：E2B 官方 README（公开）+ 本仓 `cmd/e2bgw/internal/server/server.go` 代码核实。**不含任何内网数据**
- [x] `docs/concepts/sandbox-landscape.md` — Agent 沙箱竞品格局：AgentENV/E2B/Daytona/OpenSandbox 隔离技术·启动恢复性能·E2B 兼容对比 + Substrate 定位。来源：6 篇公开 README/横评的公开事实（性能数字均来自厂商公开披露）。**不含内网进展/客户名/员工名**

## 索引与登记更新（安全写入层）

- [x] `docs/reference/index.md` — 新增 e2b-api-surface.md 行
- [x] `docs/concepts/index.md` — 新增 sandbox-landscape.md 行
- [x] `ARCHITECTURE.md` — 详细文档表补 sandbox-landscape / e2b-api-surface 链接（E2B 协议面已在此登记）
- [x] `xuanji.md` — 新增文件登记表补两篇蒸馏文档

## 移出与清理（分级门禁：整体 mv，需确认）

- [x] 整体 `mv docs/reference/collected-materials → __collected-materials-intake/`（仓根，命中 `.gitignore` 的 `__*` → 不再被跟踪）
- [x] `git rm -r --cached docs/reference/collected-materials`（解除 25 文件 git 跟踪；工作区实体已随 mv 移走）
- [x] 真删除由用户 sudo 执行（develop 无权 rm root 文件）；本命令不尝试 rm

## 验证 + 收尾（R6）

- [x] re-run `docs-utils.py --action validate`：树内 102 项违规应清零（仅剩树外 5 项容忍）
- [x] 审计日志 `--action audit --scope fanout`
- [x] 残差报告：written / moved / pending-human-decision（sudo 真删除）

## 执行顺序

`write 蒸馏文档 → 更新索引/登记 → git rm --cached + mv 移出 → validate → audit`；同名不覆盖；mv 失败即停。
