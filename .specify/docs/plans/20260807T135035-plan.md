# /speckit.docs 干跑计划 — 全量 Reconcile

- Run: `20260807T135035` · Scope: 全量（无参调用）
- 基线: Desired-State Baseline + 本地约定（宪法原则 I 双分支 rebase 存活、原则 IV 命名；本地约定优先级高于规则）

## 观察快照（R1）

- 根入口: `README.md` 252 行（>60），`CONTRIBUTING.md` 98 行（>60）；`ARCHITECTURE.md`、`CHANGELOG.md` 缺失
- `docs-utils.py validate`: 5 处违规 = 2× root-entry-oversize + 3× reserved-name-misuse（COLLABORATING.md / GOVERNANCE.md / XUANJI.md）
- `docs/`: upstream 扁平布局（13 md + `dev/` + `assets/`），无六型目录、无 `decisions/`/`notes/`
- notes scan: 空；broken links: 0；ADR: 无

## 已一致（容忍）— Principle I：upstream 文件最小 diff，超出容忍带亦不动

| 项 | 违规 | 容忍理由 |
|---|---|---|
| `README.md`（252 行） | root-entry-oversize | upstream 文件；瘦身=大面积 upstream diff，破坏 rebase 存活 |
| `CONTRIBUTING.md`（98 行） | root-entry-oversize | 同上 |
| `COLLABORATING.md` | reserved-name-misuse | upstream 治理文件；改名使 upstream 跟踪分叉 |
| `GOVERNANCE.md` | reserved-name-misuse | 同上 |
| `docs/` 扁平布局 | 不符六型 taxonomy | upstream 布局保留；taxonomy 仅约束 xuanji 新增文档 |

## 自动执行（安全写入层：建目录/建补管理文件，无同名覆盖）

- [x] `docs/decisions/index.md` — ADR 索引（append-only 规则）
- [x] `docs/decisions/template.md` — ADR 模板（status: Proposed）
- [x] `docs/notes/index.md` — notes 生命周期规则 + frontmatter 模板

## 需确认（分级门禁：搬迁/重命名 + 新增根入口，逐项可 opt-out）

- [x] 重命名 `XUANJI.md` → `xuanji.md`（xuanji 自有文件，无 upstream 冲突面；同步更新 `cmd/e2bgw/main.go` 注释引用与自身登记表行）
- [x] 新增 `ARCHITECTURE.md`（薄根入口 ≤60 行：一页架构总览，指向 `docs/` 详情）
- [x] 新增 `CHANGELOG.md`（自包含时间线：仅记 xuanji 分支变化，种子内容取自 `XUANJI.md` 与 git 事实）
- [x] 新增五型索引桩: `docs/{concepts,tutorials,tasks,reference,contribute}/index.md`（为 xuanji 增量文档预置归位目录）

## 执行顺序

`mkdir → write → mv → audit`；同名目标不覆盖；mv 失败即停并提请复核。
