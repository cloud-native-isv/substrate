---
id: "20260811T123040Z-speckit-docs"
unit_id: "/speckit.docs"
unit_type: "command"
run_id: "fanout-20260811T122511Z"
scope: "local"
partial: false
created: "2026-08-11T12:30:40Z"
summary: "扇出 intake：将 collected-materials（24 篇，18 内网原文）蒸馏为 2 篇仅公开来源的正式文档（e2b-api-surface、sandbox-landscape），并把含内网内容的原始树就地重命名为 gitignored 的 __collected-materials-intake（解除 git 跟踪）。核心风险是本仓有公开 remote，内网原文照录入库=公开泄漏"
---

## Review
扇出 intake：将 collected-materials（24 篇，18 内网原文）蒸馏为 2 篇仅公开来源的正式文档（e2b-api-surface、sandbox-landscape），并把含内网内容的原始树就地重命名为 gitignored 的 __collected-materials-intake（解除 git 跟踪）。核心风险是本仓有公开 remote，内网原文照录入库=公开泄漏；已确认尚未推到 github。两篇新文档 validate 零违规。

## Optimization Points
- validate 未按目录忽略 gitignored 的 `__*` 前缀目录：移出后 102 项违规仍来自已 gitignore 的 intake 树，噪音大。建议 docs-utils.py validate 跳过 .gitignore 命中的路径，使 validate 的信号对齐"tracked docs 空间"。
- 扇出 intake 假设可将素材 mv 出 docs 树，但 root 所有 + 无 sudo 时只能同父目录 rename；命令文档可补充"就地重命名为 gitignored 名"作为受限环境退路。
- 内网/公开来源混入 tracked 且仓库有公开 remote 时，缺少保密门禁提示；建议在 fan-out 增加"来源域 × remote 可见性"检查点。
