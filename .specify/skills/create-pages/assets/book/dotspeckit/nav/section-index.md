<!-- Generated section landing page — create-pages, stage 2 navigation completion.

This one file is mounted (read-only, never copied) as the `_index.md` of every
documentation directory that has no `index.md`. Without it Hugo treats a nested
directory as a plain URL segment instead of a section, so the sidebar loses the
grouping and the directory's own URL renders blank.

The list below is rendered from the live page tree at build time. To replace this
stub with a curated landing page, add a real `index.md` to that directory (document
content belongs to create-docs / improve-docs) and re-run the scaffolder. -->

{{< speckit-children >}}
<!-- Generated section landing page — create-pages, stage 2 navigation completion.

This single file is mounted (read-only, never copied) as the `_index.md` of every
documentation directory that has no `index.md`. Without it Hugo treats a nested
directory as a plain URL segment instead of a section, so the sidebar loses that
grouping and the directory's own URL renders blank.

The list below is rendered from the live page tree at build time. To replace this
stub with a curated landing page, add a real `index.md` to that directory (document
content belongs to create-docs / improve-docs) and re-run the scaffolder. -->

{{< speckit-children >}}
