- 每个功能域以独立 Go package 组织，包名即目录名，对外暴露简洁的函数或结构体方法。
- gRPC 相关代码统一放在 proto/ 下，通过 go:generate 自动生成 Go 实现。
- 测试文件与源码同名并以 _test.go 后缀存放，遵循 Go 标准测试约定。
- 错误处理统一通过 ateerrors 子模块定义的模型，避免直接返回裸 error。
- 上下文传递与日志记录通过 contextlogging 与 actorlog 子模块封装，禁止直接使用 log.Printf。