`envtestbins.BinaryAssetsDir` 首次调用会通过 `hack/run-tool.sh setup-envtest use --print path` 下载 kubebuilder 二进制，并使用 os.TempDir 下的固定路径进行跨进程互斥锁，避免并发 `go test ./...` 时的 text file busy 竞争；`roottest.Require` 用于标记必须由 `hack/run-root-tests.sh` 以 root 重新执行的测试。