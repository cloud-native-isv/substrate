Go 标准库 + `github.com/google/nftables` / `github.com/vishvananda/netlink` / `github.com/vishvananda/netns` 操作 Linux 内核网络栈；mTLS 使用 `crypto/tls` 并加载 kubelet 投影证书；HTTP CONNECT 隧道基于标准 `net/http`；Kubernetes 交互使用 `client-go` 的 `tools/portforward` 与 SPDY 传输。