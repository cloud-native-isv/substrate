运行前需先 source `.ate-dev-env.sh`，并通过 `go test -v ./internal/e2e/suites/... -args --e2e` 显式启用 e2e 模式；失败后使用 `hack/cleanup-e2e.sh` 清理带有 `ate.dev/e2e` 标签的残留命名空间；新套件应复制 `suites/example/testmain_test.go` 作为模板。