所有演示均通过根目录 `./hack/install-ate.sh --deploy-demo-<name>` 一键部署，卸载对应 `--delete-demo-<name>`；microvm 变体使用 `./hack/run-microvm-demo.sh` 或 `./hack/run-microvm-demo-kind.sh`；autoscaled-workerpool 需 kind 集群并使用 `install-ate-kind.sh`；claude-code-multiplex 需要设置 `ANTHROPIC_API_KEY`、`KO_DOCKER_REPO` 并通过 `docker buildx` 构建 Python 工作镜像。