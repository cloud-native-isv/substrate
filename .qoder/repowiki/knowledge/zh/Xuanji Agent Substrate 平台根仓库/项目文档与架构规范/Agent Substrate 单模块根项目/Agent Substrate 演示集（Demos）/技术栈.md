Go（counter、sandbox、multi-template/fspersist、claude-code-multiplex/ui）、Shell（load.sh、run.sh）、Kubernetes 自定义资源（WorkerPool、ActorTemplate、Actor、atespace）、ko 镜像构建、GCS 快照持久化、prometheus-adapter + HPA（autoscaled-workerpool）、Claude Code + Anthropic API（claude-code-multiplex）。