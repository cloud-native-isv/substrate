仅支持 Linux 构建（`//go:build linux`），需要云主机上已安装 `cloud-hypervisor` 和 `virtiofsd` 二进制；运行时通过 `-pod-uid`、`-cloud-hypervisor-binary`、`-kata-config`、`-kata-debug` 等 flag 指定资源路径；日志级别通过 `-log-level` 控制，atunnel 入站/出站监听地址由 `-atunnel-listen-address` 和 `-atunnel-egress-listen-address` 配置。