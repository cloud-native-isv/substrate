kubebuilder（CRD 注解与对象生成）、controller-gen（CRD YAML 输出）、client-gen/lister-gen/informer-gen（k8s.io/code-generator 工具链，生成 clientset/listers/informers）、gRPC + protobuf3（Control/Debug/ActorIdentity 服务），所有生成器通过 `hack/run-tool.sh` 包装执行。