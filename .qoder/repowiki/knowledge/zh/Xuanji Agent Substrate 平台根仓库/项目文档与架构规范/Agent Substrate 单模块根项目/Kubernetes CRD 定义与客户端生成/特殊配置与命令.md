运行 `go generate ./pkg/api/v1alpha1/...` 会依次执行 controller-gen、client-gen、lister-gen、informer-gen，需要 `../../../hack/run-tool.sh` 可用；运行 `go generate ./pkg/proto/ateapipb/...` 会通过 `protoc.sh` 生成 gRPC 代码。CRD YAML 输出目标为 `manifests/ate-install/generated/`。