二进制以 `atenet router` 和 `atenet dns` 两个子命令运行；router 模式需配置 `--ateapi-ca-file`、`--ateapi-client-cert` 或 `--ateapi-use-token-auth` 之一，默认监听 HTTP 8080、HTTPS 8443、xDS 18000、ext_proc 50051、metrics :9090、status :4040。