运行验证与更新需先确保 `GOBIN` 中可通过 `hack/run-tool.sh` 找到对应二进制；`verify-all.sh` 与 `update-all.sh` 会依次执行 `hack/verify` 与 `hack/update` 下所有脚本；GCP 相关工具需要已配置 application-default credentials 并设置 `PROJECT_ID`、`PROJECT_NUMBER`、`GCE_REGION` 环境变量。