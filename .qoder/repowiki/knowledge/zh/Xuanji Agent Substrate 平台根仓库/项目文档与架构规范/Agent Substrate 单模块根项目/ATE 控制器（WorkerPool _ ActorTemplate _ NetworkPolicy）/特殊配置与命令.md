`go:generate` 指令在 `internal/controllers/gen.go` 中定义，执行 `../../../../hack/run-tool.sh controller-gen rbac:...` 将 `//+kubebuilder:rbac:` 注释转换为 `manifests/ate-install/generated/` 下的 RBAC 清单。启动时需传入 `--ateapi-*` 系列参数（CA、客户端证书或 Token 认证）以及若干 `--otel-*` 参数，这些值会透传到 ateom worker 容器的环境变量。