- 所有外部依赖（Redis、Kubernetes、gRPC）通过函数返回的实例在 main 中集中构造后以参数形式注入各子服务，避免全局变量
- 错误处理统一使用 serverboot.Fatal 包装上下文信息并退出进程，不向上冒泡
- gRPC 服务端通过 ChainUnaryInterceptor/ChainStreamInterceptor 组合认证、限流、追踪等横切逻辑
- 资源监听通过 externalversions.SharedInformerFactory + Listers 模式获取只读视图，变更通过 Informer 回调驱动状态同步