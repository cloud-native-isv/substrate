需要预先准备三组密钥文件路径并通过 `New` 构造函数注入：`actorIDJWTPoolFile`（actor JWT 签名密钥池）、`actorIDCAPoolFile`（actor 证书 CA 池）、`workerCACerts`（atelet 工作节点 CA）。JWT 签发使用 `localjwtauthority.Unmarshal` 解析 JSON 格式的密钥池，CA 签发使用 `localca.Unmarshal` 解析 CA 池。