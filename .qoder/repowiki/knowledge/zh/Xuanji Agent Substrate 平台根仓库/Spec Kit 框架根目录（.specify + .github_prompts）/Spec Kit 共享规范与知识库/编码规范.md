- 每个概念文档在顶部声明自身为“单一事实源”，其他文件通过相对路径引用而非重复定义。
- 工作流步骤文档统一采用“步骤标题 + 行为矩阵 + 门控条件”的结构，并在关键分支处标注运行时模式门控。
- Agent 相关文档严格区分 Template / Instance / Execution 三个层次，并以表格形式映射到现有框架词汇。
- 共享常量类文档使用固定命名（如 clarify-taxonomy.md、ignore-patterns.md），便于被脚本按名称检索。
- 项目级产出（plans、audit）置于 `.specify/docs/` 下并按 ISO 时间戳前缀命名，与共享规范分离。