- 模板使用全大写方括号占位符（如 `[REQUIREMENTS_KEY]`、`[FEATURE_ID]`、`[DATE]`）标记待替换字段，命令执行时原地替换而非追加副本。
- 每个 prompt 文件遵循固定章节结构：User Input → Glossary → Outline → Phases → Key rules → Feedback → Documentation → Handoffs，并通过 `$ARGUMENTS` 接收用户参数。
- AI 工作流通过调用 `.specify/scripts/bash/create-new-plan.sh --json` 等脚本获取 JSON 上下文，再据此读取/写入 `.specify/specs/`、`.specify/memory/` 下的规范与记忆文件。
- 模板中嵌入 HTML 注释块作为 ACTION REQUIRED 指引，明确说明命令应如何填充该节（如 Constitution Check 动态推导原则表）。
- 提示词与模板之间通过 `AUTO-GENERATED` 头注释建立镜像契约，禁止直接编辑 `.github/prompts/` 下的副本，统一由 Python 脚本从模板源再生。
- 生成的文档采用编号化条目（如 CHK001、CHK002）与分类标题组织 checklist，便于追踪与引用。