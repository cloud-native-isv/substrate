Bash 脚本依赖环境变量 `CWS_LIB_BASH_HOME` 指向已安装的 `cws-lib-bash` 库（`common.sh` 会 source `${CWS_LIB_BASH_HOME}/bin/cws_bash_env` 并校验其存在）；Node 脚本需 Node ≥22.20，通过 `node scripts/js/better-harness/session-analysis.mjs --platform qoder|codex|claude|cursor|opencode --workspace <path>` 调用；Python 门禁脚本通过 `python3 scripts/python/gate-check.py --stdin` 配合 `git diff --name-only` 在 CI 中执行。